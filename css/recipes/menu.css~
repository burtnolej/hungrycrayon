
	
#content {
	margin-left: 107px;
}

.navbar	{ /* main navigator element */
   padding: 0; /* switch off padding */
	margin: 0; /* switch off the margin */
	position: fixed; /* always show nav */
    z-index: 12;
    top:22px;
	}
	
.navbar li 	{ /* top level menu item elements in the nav */
	height:20px; 
	width: 100px;  /* Each menu item is 150px wide */
	text-align: center;  /* All text is placed in the center of the box */
	list-style: none;  /* Removes the default styling (bullets) for the list */
	font-family: Arial, Helvetica, sans-serif;
	font-weight: bold;
	font-size:80%;
	margin: 0;
	background-color: #FEFAFA;
	border-bottom: 2px solid #FFF;
}

.navbar a	{ /* hyperlink text in the top level menu items */					
		padding: 2px 0;
		border:0;
		text-decoration: none;  /* Removes the default hyperlink styling. */
		color: #000; /* Text color is black */
		display: block;
		}

.navbar li:hover, a:hover {
    background-color: #FFEFEF;   
}

.navbar li ul 	{ /* elements for 2nd level down menu items */
	font-family: Arial, Helvetica, sans-serif;
	font-size:120%;
	display: none; /* Hides the drop-down menu */									
	margin: 0; /* Aligns drop-down box underneath the menu item */
	padding: 0; /* Aligns drop-down box underneath the menu item */		
   margin-left:100px;
   float:left;
   margin-top: -19px;
   height: 0px;
   	background-color: #FEFAFA;
   	border-bottom: 2px solid #FFF;
}				

.navbar li:hover ul 	{
	display: block; /* Displays the drop-down box when the menu item is hovered over */
	z-index: 12;
	padding-left: 1px;
	background-color: #FFEFEF;
	border-bottom: 2px solid #FFF;
}

.navbar li ul li a 	{
        z-index: 1001;
		}

.navbar li ul li a:hover {
    background-color:#FFEFEF;
    z-index: 1000;
    padding-bottom: 4px;
    border-bottom: 2px solid #FFF;
}